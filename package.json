{
  "name": "skillcircle",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "dev:api": "npm --prefix backend run dev",
    "build": "npm install --prefix backend --include=dev && npm --prefix backend run build",
    "build:api": "npm install --prefix backend --include=dev && npm --prefix backend run build"
  }
}
